{
  "name": "discord",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --poll src/index.ts && npm run prisma",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "prisma": "cd src/v1/database && npx prisma init",
    "prismaMigrateDev": "npx prisma migrate dev --schema=./src/v1/database/schema.prisma --name my-first-migrate",
    "resetPrismaData": "npx prisma migrate reset --schema=./src/v1/database/schema.prisma",
    "prismaPush": "npx prisma db push --schema=./src/v1/database/schema.prisma"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.14",
    "prisma": "^4.4.0",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity": "^3.186.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.186.0",
    "@aws-sdk/client-s3": "^3.186.0",
    "@aws-sdk/credential-provider-cognito-identity": "^3.186.0",
    "@prisma/client": "^4.4.0",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "express-validator": "^6.14.2",
    "secure-remote-password": "^0.3.1"
  }
}
